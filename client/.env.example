# Luc dev de trong cung duoc — Vite proxy /api sang localhost:3000 (xem vite.config.ts).
# Khi deploy len Vercel thi dien URL server that:
# VITE_API_URL=https://tl-games-server.onrender.com
VITE_API_URL=
