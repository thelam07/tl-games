# Copy file nay thanh .env roi dien gia tri that. KHONG commit file .env len git.

# Supabase > Project Settings > Data API
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

# Dung SERVICE ROLE key (chi o server, tuyet doi khong de lo ra client)
SUPABASE_SERVICE_KEY=eyJhbGciOi...

# Chuoi bi mat de ky JWT. Tu dat, cang dai cang tot.
# Tao nhanh: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=doi-chuoi-nay-di
JWT_EXPIRES_IN=7d

PORT=3000
CLIENT_ORIGIN=http://localhost:5173
